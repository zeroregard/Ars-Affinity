package com.github.ars_affinity.client.screen.perk;

public enum ConnectionType {
    AVAILABLE,      // Dotted line, school color
    ACTIVE,         // Solid line, bright school color with glow
    LOCKED,         // Gray dashed line
    PREREQUISITE    // Thick line, highlighted
}
